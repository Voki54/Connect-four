// import 'auth_api.dart';
// import 'auth_models.dart';

// class AuthRepository {
//   final AuthApi api;

//   AuthRepository({required this.api});

//   Future<JwtResponse> login(LoginRequest request) => api.login(request);

//   Future<JwtResponse> register(RegisterRequest request) => api.register(request);

//   Future<JwtResponse> refreshToken(RefreshTokenRequest request) => api.refreshToken(request);

//   Future<void> logout(String token) => api.logout(token);
// }
